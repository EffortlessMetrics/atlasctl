---
status: complete
lane: source-of-truth-stack
owner: atlasctl
date: 2026-06-02
---

# v0.1-proof-topology Closeout Checkpoint

## Scope Completed in Current Stack
- Parser compatibility: canonical `relation` with legacy `kind` support and strict conflict/missing validation.
- Structural cleanup: removed `atlasctl-codes` and `atlasctl-ports`; moved semantics into `atlasctl-types` and `atlasctl-app`.
- Graph semantics: owns/touches ownership model with role-aware validation and source-of-truth stack node coverage.
- Operational UX: strengthened `doctor`, `impacted`, and `why` context and review packet rendering.
- Protocol hardening: updated JSON schema versions + golden compatibility coverage for atlas/impact/why surfaces.
- Discovery enhancements: active goal manifest, policy-ledger TOML, support-tier/claim metadata, and scoped diagnostics.
- Real-repo dogfood scorecard captured in `docs/handoffs/atlasctl-real-repo-dogfood.md`.
- Added explicit support-tier and policy-ledger source-of-truth metadata for README/docs commitments.

## Evidence from this branch
- `rtk cargo test -p atlasctl-cli --test cli_integration`
- `rtk cargo run -p atlasctl-cli -- check --profile ci --repo-root .`
- `rtk cargo run -p xtask -- ci-full`
- `rtk cargo run -p xtask -- schema --check`
- `rtk cargo run -p atlasctl-cli -- doctor --profile ci --repo-root .`
- `rtk cargo run -p atlasctl-cli -- impacted --base main --head HEAD --format review-packet --repo-root .`
- `rtk cargo run -p atlasctl-cli -- why --path crates/atlasctl-core/src/lib.rs --format markdown --repo-root .`
- External portability check against `H:\Code\Rust\tokmd`:
  - base: `4b431559ee2cf3eda81b499c1cf678d9ab2fde6a`
  - head: `95a5034abeeb1552ce10432647a19bff2b98b08a`
  - `rtk cargo run -p atlasctl-cli -- doctor --repo-root H:\Code\Rust\tokmd --profile ci`
  - `rtk cargo run -p atlasctl-cli -- check --repo-root H:\Code\Rust\tokmd --profile ci`
  - `rtk cargo run -p atlasctl-cli -- impacted --repo-root H:\Code\Rust\tokmd --base 4b431559ee2cf3eda81b499c1cf678d9ab2fde6a --head 95a5034abeeb1552ce10432647a19bff2b98b08a --format review-packet`
  - `rtk cargo run -p atlasctl-cli -- impacted --repo-root H:\Code\Rust\tokmd --base 4b431559ee2cf3eda81b499c1cf678d9ab2fde6a --head 95a5034abeeb1552ce10432647a19bff2b98b08a --format json`

## Lane posture
- This branch has completed the `v0.1-proof-topology` closeout scope captured at handoff time.
- Remaining work is on future refinements (scope-split hygiene and additional real-repo transferability tests) rather than closeout blockers.
- External portability was demonstrated on `tokmd` with structural discovery and scope-aware diagnostics; proof-chain (`why`) output is expectedly empty until `atlas.toml` metadata is supplied.

## Suggested next actions
- Keep review packets fresh when claims/coverage changes.
- Extend dogfood checks to additional external repositories to validate portability variance.
- Add explicit onboarding guidance for external repos to declare `atlas.toml` coverage metadata early so `why` can return non-empty proof chains.
