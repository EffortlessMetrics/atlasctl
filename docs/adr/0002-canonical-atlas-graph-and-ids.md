---
atlas:
  id: adr:0002-canonical-atlas-graph-and-ids
  kind: adr
  title: Canonical atlas graph and IDs
  explains:
    - req:deterministic-atlas
---
# ADR 0002: Canonical atlas graph and IDs

The atlas uses typed nodes, typed edges, and stable human-readable IDs.

IDs use `<kind>:<slug>`.

The canonical output is `atlas.json`.
